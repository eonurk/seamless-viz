# seAMLess frontend

The React/Vite dashboard for the standalone seAMLess stack.

```bash
npm install
npm run dev
```

The frontend expects the API service at `http://localhost:3001/v1` unless
`VITE_API_BASE_URL` is set. The full local workflow is documented in
[`../docs/DOCKER.md`](../docs/DOCKER.md).
